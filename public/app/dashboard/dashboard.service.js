(function () {

  "use strict";
  var app;
  app = angular.module('textserv');

  app.factory('dashboardSvc', ['$http', '$log', function dashboardSvc($http, $log) {

    return {
    };
  }]);

}());
